<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is REQUIRED: OSP peering runs plain HTTP on the LAN, and the
     integrity layer is the protocol's own packet signature (Ed25519 + TLS
     transport is the production path). Without it Android 9+ blocks
     HttpHub entirely ("Cleartext HTTP traffic not permitted"). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Package visibility (Android 11+): we bind the LLMProvider service. -->
    <queries>
        <package android:name="com.tree4five.gguf" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.Tree4Five.OSP"
        android:usesCleartextTraffic="true">

        <!-- Persists the per-app locale chosen in the Language submenu across
             process death (appcompat's backport storage, below API 33). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".OspService"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="com.swarmknowledge.ospbridge.ACTION_OSP_SERVICE" />
            </intent-filter>
        </service>
    </application>
</manifest>
